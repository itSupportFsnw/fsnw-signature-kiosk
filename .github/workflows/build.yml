name: Build Plugin ZIP

on:
  push:
    branches: [ main, develop ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: composer

      - name: Install Composer dependencies (production)
        run: composer install --no-dev --optimize-autoloader --prefer-dist --no-progress

      - name: Build plugin ZIP
        run: |
          mkdir -p build/fsnw-signature-kiosk
          rsync -a --exclude-from='.distignore' ./ build/fsnw-signature-kiosk/
          (cd build && zip -r fsnw-signature-kiosk.zip fsnw-signature-kiosk)

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: fsnw-signature-kiosk
          path: build/fsnw-signature-kiosk.zip
